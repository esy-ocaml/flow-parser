{
  "name": "@esy-ocaml/flow-parser",
  "version": "0.36.0",
  "description": "esy package for flow-parser",
  "license": "MIT",
  "esy": {
    "build": [
      [ "sh", "-c", "cd src/parser && make ocamlfind-install" ]
    ],
    "install": [
      "esy-installer"
    ],
    "buildsInSource": true
  },
  "dependencies": {
    "@esy-ocaml/esy-installer": "^0.0.0",
    "@opam/ocamlfind": "*",
    "ocaml": "^4.2.x",
    "@opam/ocamlbuild": "*"
  },
  "devDependencies": {
    "@esy-ocaml/merlin": "*",
    "ocaml": "~4.6.0"
  }
}
